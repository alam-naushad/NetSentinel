# ML workspace

The next increment will add reproducible loading, validation, preprocessing, and baseline experiments here.

Training artifacts must be versioned as a set: preprocessing pipeline, feature schema, decision-threshold configuration, model metadata, and evaluation metrics. Raw benchmark data remains outside Git under `data/raw/`.
